---
id: G-033
title: Build audio waveform above the timeline (implements D-002)
status: done
serves-design: D-002
success-criteria:
  - The Split screen shows the loaded video's audio waveform as a band above the timeline, aligned to the same 0..duration x-axis
  - Vector-drawn, themed, crisp on resize; playhead + cut-marker ticks align on it; clicking the wave seeks (fused)
  - Peaks extracted in the background on load + cached (best-effort, never blocks/crashes); no-audio → band hidden
  - Every existing command/binding intact; tests green
tasks: [T-083, T-084, T-085]
proceed-with: perfect-dev --auto-approve
created: 2026-07-19
---

## Why
Implements the sealed design **D-002** (`docs/design/D-002-audio-waveform.md`) — see the audio (speech/silence)
to place cut points. Mirrors the G-030 hover-thumbnail feature's Core→App shape.

## Plan
```
T-083 Core IWaveformService — ffmpeg → downsampled mono PCM temp → normalized peak array, LRU cache, best-effort, cancel + tests
T-084 App — WaveformViewModel + TimelineView waveform band (fused playhead/markers/click) + background wiring in LoadAsync
T-085 docs — CHANGELOG + CLAUDE convention; mark D-002 built
```

## Reference
Full model: `docs/design/D-002-audio-waveform.md` (decisions D1 vector · D2 fused · D3 Split-only). Grounded in
`TimelineView.xaml` Track (the mark bar), `SplitView.xaml` Row 2, the keyframe-scan background pattern, and the
`IThumbnailService`/`FfmpegThumbnailService` pattern (T-077) for the ffmpeg-temp-file + LRU-cache approach.
