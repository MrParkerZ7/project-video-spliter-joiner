---
id: D-004
kind: design
title: Bulk Cut tab — batch intro + optional-outro trim (keep the middle, stream-copy, many videos at once)
status: confirmed
concept: >
  A third screen tab "Bulk Cut" (SelectedTabIndex==2, sibling to Split/Join) that trims the long intro (and optional
  outro) off MANY videos at once to shrink files — WITHOUT re-encoding. A list of videos, each a row with a mini
  scrub bar carrying a gold intro-end handle + an optional blue outro-start handle; keep the middle
  [intro-end -> outro-start] (or -> EOF), keyframe-snapped -c copy. "Apply cut points to all" copies one row's points
  to the rest (same-series in one click). Output = <source folder>/<name>_trimmed<ext>; originals kept. A bulk trim IS
  a Split that keeps one middle segment — reuses SplitEngine/SplitRequest/CutMarkerViewModel/OperationViewModel + Join's
  list pattern wholesale; adds no second ffmpeg path.
open-questions: []
decisions-resolved-at-seal:
  - "Apply-to-all outro semantics = time-from-END (Duration − outroStart); intro-end = absolute time-from-start. (Recommended default, accepted at seal — flippable during the build.)"
  - "Output collision default = AutoSuffix (_trimmed_2/_3), never clobber, per-run Overwrite toggle. (Recommended default, accepted at seal.)"
artifacts:
  dir: docs/design/D-004/
  doc: docs/design/D-004/README.md
  flow: docs/design/D-004/core-flow.md
goal-with: todo-task
serves: ''
created: 2026-08-13
updated: 2026-08-13
---

# D-004 — Bulk Cut tab (batch intro + optional-outro trim)

**Board index for the design.** Full spec: [`../design/D-004/README.md`](../design/D-004/README.md) ·
flow + edge matrix: [`../design/D-004/core-flow.md`](../design/D-004/core-flow.md).

## Snapshot
- **What:** a 3rd tab "Bulk Cut" — batch-trim the intro (+ optional outro) off many videos, keeping the middle,
  stream-copy + keyframe-snapped. Output `<name>_trimmed<ext>` beside each source; originals kept.
- **How (reuse):** a bulk trim = a Split keeping ONE middle segment. Reuses `SplitEngine.SplitAsync` /
  `SplitRequest` / `MediaProbe` snap / `CutMarkerViewModel` (×2 handles/row) / `OperationViewModel` (per-row + aggregate)
  / Join's list scaffold / `IThumbnailService`. New: `BulkCutViewModel`, `BulkItemViewModel`, `BulkCutView`, an optional
  Core `BulkTrimEngine`+`KeptSegmentSelector`, and one new token `DropScrimBrush`.
- **UI:** per-row dual-handle scrub bar (gold intro-end `▸` top-cap · blue outro-start `◂` bottom-cap · gold keep-span
  glow · scrimmed dropped spans); apply-to-all; the four reused lifecycle surfaces; taskbar/title via the aggregate op.

## Clarified decisions (locked)
1. Cut points = **visual per-video + apply-to-all**.
2. Scope = **intro + optional outro** (keep the middle; outro optional ⇒ keep→EOF).
3. Output = **same folder, `_trimmed` suffix, originals kept**.

## Status
`confirmed` (sealed 2026-08-13 via `todo-design-done`). Both open-questions resolved to their recommended defaults
(see `decisions-resolved-at-seal`). Auto-planned into epic **G-036** (tasks T-094…T-098). No `docs/specs/` living-spec
structure in this target → spec-seed skipped (target convention is design-folder + board). Drive with **`todo-next-all`**.
