---
id: T-151
title: Cut v1.2.0 — release prepared; the tag is yours to push
status: blocked
when: next
priority: high
estimate: M
serves: none
blocked-by: [user-action]
blocks: []
source: todo-next-flow discovery
spec: n/a
lenses: [tech-lead, product-owner]
created: 2026-09-02
---

## What is wrong

Two problems that only look like one.

**1. The work is unreleased.** 25 commits sit after `release: v1.1.0` (3188328), 17 of them `feat`/`fix`:
delete-originals after a batch, profile backup/restore, the delete-originals handle release, Exact cut
verified on real media, the test-settings-pollution fix, last-tab restore, and four Bulk Cut layout
fixes. `CHANGELOG.md`'s `## [Unreleased]` section is **empty**, `Directory.Build.props` still reads
`<Version>1.1.0</Version>`, and root `CLAUDE.md` is staler still (says 0.1.0).

**2. No tag has ever existed.** `git ls-remote origin` returns exactly two refs: `HEAD` and
`refs/heads/main`. **Zero tags**, local or remote; `git describe --tags` fails with "No names found".
`.github/workflows/release.yml` triggers only on `push: tags: ['v*.*.*']`, so it has **never executed** —
for v1.0.0 or v1.1.0 — despite CHANGELOG and ROADMAP recording both as shipped.

## The concrete failure it allows
Cutting the next release by pushing a tag would be the **first ever end-to-end run** of that workflow. It
would (a) pass its `Test (release gate)` step vacuously (see [[T-149]]), then (b) hit whatever is actually
broken in a publish path nobody has executed. Meanwhile `generate_release_notes: true` on a main-only
history produces a commit dump rather than the curated CHANGELOG that already exists.

## Design
1. Land [[T-149]] first — releasing behind a gate that cannot fail is worse than not releasing.
2. Fill `## [Unreleased]` from the 17 commits, in the CHANGELOG's existing voice (user-facing outcomes,
   not commit subjects). Bump `Directory.Build.props` to 1.1.1 and fix the stale version in `CLAUDE.md`.
3. **Dry-run the release workflow before tagging** — `act`, a throwaway pre-release tag, or a
   `workflow_dispatch` path — so its first real execution is not also its first test.
4. Then tag and push.

## Acceptance criteria
- [x] `[Unreleased]` emptied into a full **`[1.2.0]`** section — 7 Added, 7 Fixed, 3 Internal, in the
      file's existing voice (what the user sees and why it mattered, not commit subjects).
- [x] `Directory.Build.props` → `1.2.0`, verified stamped on the built exe
      (`1.2.0+f3865ef`). `CLAUDE.md:224` corrected — it still claimed `0.1.0`.
- [ ] **Not done — needs you.** See below.
- [ ] **Not done — needs you.** Pushing a tag publishes a GitHub release; that is not mine to trigger.

## Out of scope
Changing the release mechanism. It just has to be run.

## Status — 2026-09-02: prepared, stopped at the tag

Everything local and reversible is done. The remaining steps publish something to the world, so they stop
here rather than proceeding under a drive-to-completion flag.

### It is 1.2.0, not 1.1.1
The ticket said patch. The 19 user-facing commits include real **features** — profile backup/restore
(G-051), delete-originals (G-050), snapshot thumbnails (G-047), batch-wide apply and the run-scope
summary (G-046), remembered tab (G-049). Semver makes that a **minor**. Renamed accordingly.

### Done
- `CHANGELOG.md` `[1.2.0]` written from the 19 commits, in the file's voice.
- `Directory.Build.props` → `1.2.0`; confirmed on the built binary as `1.2.0+f3865ef` rather than assumed.
- `CLAUDE.md` version note corrected (it read `0.1.0`).
- **[[T-149]] landed first**, so the gate this release passes through can now actually fail — that was the
  precondition, and releasing without it would have meant trusting a gate that reported green without
  running.
- Suite **1315 green**.

### What is left, and why it is yours

**No tag has ever existed in this repo** — `git ls-remote origin` returns only `HEAD` and
`refs/heads/main`. `.github/workflows/release.yml` triggers solely on `push: tags: ['v*.*.*']`, so it has
**never executed**, for v1.0.0 or v1.1.0, despite both being recorded as shipped. Pushing `v1.2.0` would
be that workflow's first ever run, and it publishes a public GitHub release with installer artifacts.

That is outward-facing and hard to undo, so it is a decision rather than a step. Two ways to take it:

```
# Option A — rehearse first (matches this ticket's design)
git tag v1.2.0-rc1 && git push origin v1.2.0-rc1     # exercises release.yml end to end, throwaway
# inspect the run, then delete the pre-release + tag, and cut the real one

# Option B — cut it directly
git tag v1.2.0 && git push origin v1.2.0
```

One thing to expect either way: `release.yml` passes `generate_release_notes: true`, which on a
main-only history produces a commit dump rather than the curated `[1.2.0]` section that now exists. Worth
pointing the release body at CHANGELOG.md, but that is a change to make deliberately, not silently on the
way past.

Reopen and finish this once the tag is pushed and the published artifact has been installed and launched.
