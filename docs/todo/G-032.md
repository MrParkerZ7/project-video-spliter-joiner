---
id: G-032
title: Build vertical-monitor mode (implements D-001)
status: done
serves-design: D-001
success-criteria:
  - A title-bar toggle button flips the app between horizontal (two-column) and vertical (stacked) layout on both Split and Join
  - Vertical = video+timeline on top (full width), tool panel stacked below (scrollable); the GridSplitter rotates to a horizontal divider
  - LayoutMode persists across launches (AppSettings); the app reopens in the last-used mode
  - Per-axis splitter ratio remembered; every existing command/binding intact; tests green
tasks: [T-081, T-082]
proceed-with: perfect-dev --auto-approve
created: 2026-07-19
---

## Why
Implements the sealed design **D-001** (`docs/design/D-001-vertical-monitor-mode.md`). The app's two-column
layout suits landscape monitors; a toggle to a stacked vertical layout suits portrait monitors.

## Plan
```
T-081 build the feature — AppSettings.LayoutMode + MainViewModel toggle + caption toggle button + the
      axis-aware container (Split & Join flip column↔row) + per-axis splitter ratio + tests
T-082 docs — CHANGELOG + CLAUDE.md convention; mark D-001 built
```

## Reference
Full model: `docs/design/D-001-vertical-monitor-mode.md` (decisions D1–D6). Grounded in `SplitView.xaml`/
`JoinView.xaml` 3-column Grid, `MainWindow.xaml` caption row, `AppSettings`, the G-029 vector caption-icon style.
