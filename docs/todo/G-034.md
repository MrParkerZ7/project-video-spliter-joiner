---
id: G-034
title: Split tool-panel layout refinements (side-by-side lists + Load/Clear onto the tab line)
status: active
success-criteria:
  - The cut-positions list and the "Parts to export" list share horizontal width (side by side) instead of stacking full-width
  - Load / Clear move onto the Split/Join tab-strip line (top-right), reclaiming a line in each tool panel
  - Both play well with D-001 vertical mode (narrow horizontal panel vs full-width vertical panel) and don't break any command
tasks: [T-087, T-088]
proceed-with: perfect-dev --auto-approve
created: 2026-07-19
---

## Why
Two user-requested Split-screen space refinements (2026-07-19): pack the tool panel tighter — two lists sharing
the horizontal width, and Load/Clear promoted to the otherwise-empty tab-strip line.

## Plan
```
T-087 cut-positions list + Parts-to-export list share horizontal size (side-by-side columns)
T-088 move Load / Clear onto the Split/Join tab-strip line (top-right), bound to the active screen
```

## Reference (grounded 2026-07-19)
Split tool panel = `SplitView.xaml` ScrollViewer→StackPanel: Load/Clear row (`:130-141`) → file-info card →
**Cut markers** section (header + "Add cut at playhead" + a **POSITION** field row `:216-228` + marker list `:231`)
→ **Parts to export** section (`:266` header+chips + segments list `:294`) → Output → Run. The Split/Join tabs are a
`TabControl` in `MainWindow.xaml` (`:231-239`) with empty space right of the two `TabItem` headers. `MainViewModel`
already exposes `SelectedTabIndex` + `CurrentOperation` (T-068) — the pattern for routing to the active screen.
