---
id: T-022
title: Bring docs current with FFME preview (perfect-docs)
status: dropped
superseded-by: T-026
when: last
priority: low
estimate: S
serves: G-004
blocked-by: [T-019, T-020, T-021]
blocks: []
source: todo-goal-next
spec: reviewed
proceed-with: perfect-docs --auto-approve
created: 2026-07-16
---

## Concept
Terminal node: document the FFME preview swap so docs reflect what G-004 shipped. Fixed closer.

## Acceptance criteria
- [ ] USER_GUIDE: the preview now plays via ffmpeg (FFME) → plays what the app can cut (HEVC/MKV/etc.); "preview unavailable" now only for genuinely-undecodable files.
- [ ] README: note the ffmpeg-decoded preview (no MF codec limit) + the dev-setup step to fetch the shared build.
- [ ] ARCHITECTURE: update the player layer — FFME behind `IMediaPlayer` (replaces MediaElement/D1), the ffmpeg shared build serving both FFME (DLLs) and the engine (exes), `FFmpegDirectory` init; record the resolved D1-alt decision.
- [ ] CHANGELOG: entry (0.2.0/Unreleased) for the FFME preview.
- [ ] CLAUDE: convention — one bundled ffmpeg shared build serves engine + preview; set `Library.FFmpegDirectory` at startup; native libs gitignored (dev-setup step).
- Doc layers end `✅ current` for G-004's scope.

## Dependencies
T-019, T-020, T-021.

## Out of scope
Docs for parked items (LGPL swap, hw-accel).
