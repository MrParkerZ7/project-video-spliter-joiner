---
id: T-032
title: Docs — fast video load (perfect-docs)
status: ready
when: last
priority: low
estimate: S
serves: G-008
blocked-by: [T-030, T-031]
blocks: []
source: todo-goal
spec: reviewed
proceed-with: perfect-docs --auto-approve
created: 2026-07-17
---

## Concept
Terminal node: document the fast-load behavior once G-008 ships. Fixed closer.

## Acceptance criteria
- [ ] USER_GUIDE: note that loading a video shows the preview immediately and the keyframe index builds in the background ("indexing…"); snapping is ready once indexed.
- [ ] ARCHITECTURE: the load flow (probe → open preview → background keyframe index) + the demux-level packet-flag keyframe scan (with frame-scan fallback) in `MediaProbe`.
- [ ] CHANGELOG: entry for the load performance improvement.
- [ ] CLAUDE: convention — keyframe indexing is background + non-blocking; the scan is packet-flag (demux) with a frame-scan fallback.
- Doc layers end `✅ current` for G-008 scope.

## Dependencies
T-030, T-031.

## Out of scope
Docs for parked items (windowed indexing, persistent cache).
