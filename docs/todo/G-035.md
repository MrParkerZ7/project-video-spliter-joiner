---
id: G-035
title: Build the D-003 Split tool-panel 50/50 layout
status: done
serves-design: D-003
success-criteria:
  - Position (POSITION field + Add-at-time) sits on the same line as "Add cut at playhead" in the Cut markers section
  - Cut markers section and Parts-to-export section split the tool-panel height 50/50 (stacked, star-rows, lists scroll internally)
  - Parts to export is full-width; every command/binding intact; usable at narrow width + in D-001 vertical mode; tests green
tasks: [T-090]
proceed-with: perfect-dev --auto-approve
created: 2026-08-13
---

## Why
Implements sealed design **D-003** (`docs/design/D-003-split-panel-5050.md`, decisions D1–D6).

## Plan
```
T-090 restructure SplitView.xaml — Add-cut+Position on one row; tool-panel → bounded 50/50 star-row Grid; Parts full-width + docs
```

## Reference
The real work is converting the tool-panel container from `ScrollViewer→StackPanel` to a bounded `Grid` where the
Cut-markers list + Parts list are two `*` rows (fixed elements Auto). Grounded in `SplitView.xaml` (T-089 state).
