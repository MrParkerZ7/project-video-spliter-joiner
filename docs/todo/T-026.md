---
id: T-026
title: Docs — FFME preview + auto-detect removal + 4K performance (perfect-docs; finalizes G-004)
status: done
session: app-split-join
when: last
priority: low
estimate: S
serves: G-005
blocked-by: [T-023, T-024, T-025]
completed: 2026-07-17
commit: fadecf7
blocks: []
source: todo-goal-next
supersedes: T-022
proceed-with: perfect-docs --auto-approve
created: 2026-07-16
---

## Concept
One docs pass covering everything since the last docs update: the FFME preview (G-004), the removal of auto-detect (G-005), and the 4K performance work. Closes G-004's docs (was T-022) + G-005's.

## Acceptance criteria
- [ ] USER_GUIDE: preview now decodes via ffmpeg/FFME (plays what you can cut — HEVC/MKV/4K); **remove the auto-detect section** (feature gone); note 4K plays smoothly (HW-accel/downscaled preview) and that cuts are always stream-copy (resolution-independent).
- [ ] README: features updated — ffmpeg-decoded preview, no auto-detect, 4K support; note the dev-setup step to fetch the ffmpeg shared build.
- [ ] ARCHITECTURE: player layer = FFME behind `IMediaPlayer` (replaces MediaElement); the one bundled ffmpeg shared build serving preview (DLLs) + engine (exes); **remove the Detect layer** from the description; record the 4K preview strategy (HW-decode + downscale) + D1-alt decision.
- [ ] CHANGELOG: entries for FFME preview, auto-detect removal, 4K performance (0.2.0/Unreleased).
- [ ] CLAUDE: conventions — one ffmpeg shared build for preview + engine; `Library.FFmpegDirectory` at startup; no auto-detect; preview is downscaled/HW-accelerated, never the cut.
- [ ] **Also cover G-006 (adjustable video pane):** USER_GUIDE note that the video preview area is drag-resizable (GridSplitter); README/ARCHITECTURE mention if relevant.
- [ ] **Also cover G-007 (player controls):** USER_GUIDE — the skip buttons (±1s/5s/10s/20s/1m/5m), frame-step, jump-to-ends for finding the exact split point, + volume/mute/playback-speed; README feature list.
- [ ] Confirm no dangling references to the removed auto-detect in any doc.
- Doc layers end `✅ current` for G-004 + G-005 + G-006 + G-007 scope.

## Dependencies
T-023, T-024, T-025.

## Out of scope
Docs for parked items (LGPL swap, smarter detector, hw-encode).
