---
id: T-070
title: Docs — taskbar progress + per-part split progress
status: ready
when: after
priority: medium
estimate: S
serves: G-025
blocked-by: [T-068, T-069]
blocks: []
source: todo-goal
spec: reviewed
proceed-with: perfect-dev --auto-approve
created: 2026-07-18
---

## Concept
Document the taskbar-button progress + window-title ETA (T-068) and the per-part split progress (T-069).

## Design
- **CHANGELOG.md [0.2.0] Added:** an entry for taskbar-button progress + ETA-in-title, and one for per-part split progress (both split paths).
- **CLAUDE.md "Conventions":** add a convention capturing (a) the taskbar `TaskbarItemInfo` binding to `Operation.Progress`/`TaskbarProgressState` and the window-title ETA pattern, and (b) the per-part reporting channel + the "fast muxer path stays single-pass, per-part is DERIVED from ffmpeg time via the pure boundary-mapping function" invariant (so a future change doesn't switch the fast path to per-segment for progress).

## Acceptance criteria
- [ ] CHANGELOG entries added under [0.2.0].
- [ ] CLAUDE.md conventions updated (taskbar + per-part-derivation invariant).
- [ ] Matches what T-068/T-069 actually shipped.

## Dependencies
T-068 + T-069.

## Out of scope
New behavior.
