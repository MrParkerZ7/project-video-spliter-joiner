---
id: T-117
title: Docs + spec update — Bulk row-selection debounce + apply-to-all discoverability
status: done
when: after
priority: low
estimate: S
serves: G-040
blocked-by: [T-115, T-116]
completed: 2026-08-24
commit: bced386
source: todo-task
spec: reviewed
lenses: [product-owner]
created: 2026-08-23
---

## Concept
Document the G-040 fixes and keep SPEC-011 current: the deferred/debounced preview-open on row selection and the
clearer apply-to-all affordances.

## Design
1. **`docs/specs/SPEC-011-bulk-cut-screen.md`** — add invariants: selecting a row updates highlight/`HasSelection`/command
   states IMMEDIATELY while the preview `Player.Open` is **debounced + latest-wins** (rapid selection opens only the settled
   row; null/clear cancels the pending open) — grounded in the shipped T-115 + its test. Note the apply-to-all affordances
   (per-row ⧉ + profile) are visible/labelled in both layout modes (T-116 is largely visual — add an invariant only if a
   testable behavior changed, else a note). Refresh `sources:`/`updated:` + `docs/specs/_index.md` counts if invariants added.
2. **`CHANGELOG.md`** — an entry under Unreleased: "Bulk Cut — instant row selection (debounced preview) + clearer apply-to-all (G-040)".
3. **`docs/USER_GUIDE.md`** — a light note if wording needs it (selecting a row previews it; apply-to-all is the ⧉ on each row / in the Profiles card). Only if it improves clarity.

## Acceptance criteria
- [ ] SPEC-011 gains the debounced-preview-open invariant (grounded in T-115 + test); apply-to-all affordance noted; `_index.md` counts refreshed if invariants added.
- [ ] CHANGELOG entry added; USER_GUIDE touched only if it improves clarity.
- [ ] Docs only — no source/test changes; grounded in shipped behavior; commit explicit pathspec.

## Dependencies
Needs **T-115 + T-116** shipped so the docs/specs describe reality.

## Risks
Ground in shipped behavior (anti-fabrication). T-116 is largely visual — don't invent an invariant for a pure label change.

## Out of scope
Any code.
