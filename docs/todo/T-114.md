---
id: T-114
title: Docs + spec update — Bulk Cut layout-mode + profiles UX + apply-to-all fix
status: ready
when: after
priority: low
estimate: S
serves: G-039
blocked-by: [T-111, T-112, T-113]
source: todo-task
spec: reviewed
lenses: [product-owner]
created: 2026-08-23
---

## Concept
Document the G-039 polish and keep SPEC-011 current: the Bulk Cut tab's layout-mode responsiveness, the profiles-UI
refresh, and the apply-to-all re-activation fix.

## Design
1. **`docs/USER_GUIDE.md`** — note that Bulk Cut now adapts to the vertical/horizontal layout toggle (pane ↔ list flips),
   and refresh any profiles-area wording to match the regrouped control. Match the guide's voice.
2. **`CHANGELOG.md`** — an entry under Unreleased: "Bulk Cut — layout-mode-aware, refreshed profiles UI, apply-to-all fix (G-039)".
3. **`docs/specs/SPEC-011-bulk-cut-screen.md`** — add/refresh invariants: the OrientedSplitPanel pane↔list flip on
   `IsVertical` + remembered ratios (grounded in T-112); the apply-to-all re-invocation invariant (fires every time,
   re-reads the current source — grounded in the T-111 fix + its test); and any behavior note from the profiles-UI refresh
   (T-113 is visual, so likely no new invariant — note if so). Refresh `sources:`/`updated:` + `docs/specs/_index.md` counts
   if invariants were added (the new tests are `serves-spec:SPEC-011`-tagged so coverage stays ~current).
4. **`docs/ARCHITECTURE.md`** (optional, brief) — Bulk Cut reuses `OrientedSplitPanel` for its pane↔list split (like Split).

## Acceptance criteria
- [ ] USER_GUIDE notes Bulk layout-mode + refreshed profiles wording; CHANGELOG entry added.
- [ ] SPEC-011 gains the layout-flip + apply-to-all-fires-every-time invariants (grounded in shipped T-111/T-112); `_index.md` counts refreshed if any invariant added.
- [ ] Docs only — no source/test changes; grounded in what shipped (no aspirational claims); commit explicit pathspec.

## Dependencies
Needs **T-111 + T-112 + T-113** shipped so the docs/specs describe reality.

## Risks
Ground in the shipped behavior (anti-fabrication bar). T-113 is visual — don't invent an invariant for a pure style change.

## Out of scope
Any code; the make-testable refactors (T-105).
