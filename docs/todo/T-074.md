---
id: T-074
title: Docs — themed scrollbar + operation-state surface
status: done
when: after
priority: medium
estimate: S
serves: G-027
blocked-by: [T-072, T-073]
blocks: []
completed: 2026-07-18
source: todo-goal
spec: reviewed
proceed-with: perfect-dev --auto-approve
created: 2026-07-18
---

## Concept
Document the themed scrollbar (T-072) and the clear operation-state surface (T-073).

## Design
- **CHANGELOG [0.2.0] Added:** "Themed scrollbars (dark+gold)"; "Clear operation states — a ✓ done confirmation (with result + open folder), plus distinct cancelled/failed surfaces, instead of the running UI silently vanishing."
- **CLAUDE.md "Conventions":** note (a) the implicit `ScrollBar` style lives in `Controls.xaml` (app-wide themed chrome); (b) the operation-state surface convention — Running/Completed/Cancelled/Failed each render distinctly, driven by `OperationState` + `IsCompleted`/`IsCancelled`/`ResultSummary`, and MUST reset on a new run/load/Clear.

## Acceptance criteria
- [ ] CHANGELOG entries added under [0.2.0].
- [ ] CLAUDE.md conventions updated (scrollbar + state-surface).
- [ ] Matches what T-072/T-073 shipped.

## Dependencies
T-072 + T-073.

## Out of scope
New behavior.
